(module info (lib "infotab.ss" "setup")
  (define name "Disk")
  (define blurb
    (list "A library of disk functions. For now sorting of large files."))
  (define primary-file "disk.ss")
  (define doc.txt "doc.txt")
  (define categories '(datastructures))
  (define version "1.0")
  (define required-core-version "350"))